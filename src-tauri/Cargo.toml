[package]
name = "arcu"
version = "0.1.0"
description = "Spotlight alternative"
default-run = "arcu"
edition = "2018"
build = "src/build.rs"

[build-dependencies]
tauri-build = { version = "1.2", features = [] }

[dependencies]
serde_json = "1.0"
serde = { version = "1.0", features = ["derive"] }
tauri = { version = "1.2", features = ["global-shortcut-all", "shell-open", "system-tray", "window-start-dragging"] }
cpc = "1.9"

[target.'cfg(target_os = "macos")'.dependencies]
cocoa = "0.24"
objc = "0.2"

[features]
default = [ "custom-protocol" ]
custom-protocol = [ "tauri/custom-protocol" ]
